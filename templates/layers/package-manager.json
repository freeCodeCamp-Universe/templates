{
  "bun": {
    "devCmd": [
      "bun",
      "run",
      "dev"
    ],
    "files": {
      ".dockerignore": "node_modules\ndist\n",
      "bunfig.toml": "[install]\n# Only install package versions published at least 3 days ago\nminimumReleaseAge = 259200 # seconds\n"
    },
    "lockfile": "bun.lock",
    "manifests": [
      "package.json",
      "bunfig.toml"
    ],
    "pmInstall": "RUN npm install -g bun@{{pmVersion}}",
    "pmVersion": "1.3.14"
  },
  "pnpm": {
    "devCmd": [
      "pnpm",
      "run",
      "dev"
    ],
    "files": {
      ".dockerignore": "node_modules\ndist\n",
      "pnpm-workspace.yaml": "blockExoticSubdeps: true\nminimumReleaseAge: 4320 # packages under 3 days old are simply rejected.\ntrustPolicy: no-downgrade\ntrustPolicyIgnoreAfter: 20160 # packages between 3 and 14 days old with no signature are also rejected.\nengineStrict: true\n"
    },
    "lockfile": "pnpm-lock.yaml",
    "manifests": [
      "package.json",
      "pnpm-workspace.yaml"
    ],
    "pmInstall": "RUN corepack use pnpm@{{pmVersion}} && corepack enable pnpm",
    "pmVersion": "10.34.1",
    "preinstall": "npx only-allow pnpm"
  }
}
